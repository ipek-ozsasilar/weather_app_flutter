//flutter
import 'package:flutter/material.dart';

//radius sınıfı
class weatherBorderRadiusClass {
  static BorderRadius iconButtonRadius = BorderRadius.circular(25);
  static BorderRadius containerRadius = BorderRadius.circular(20);
  static BorderRadius textFieldRadius = BorderRadius.all(Radius.circular(20));
}
